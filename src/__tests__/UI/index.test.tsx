import React from 'react'
import {screen,render} from '@testing-library/react'
import Canvas from '@components/App'

const sum = (one:number,two:number)=>one+two

describe("some placeholder test", () => {

	test("should display a blank login form, with r",()=>{
		// render(<Canvas/>)

		// const canvas = screen.getByRole('canvas')
		// console.log(canvas)

	});

});