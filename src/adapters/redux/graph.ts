import store, { InitialState } from '../../redux/store'
import {
	ALLOW_ADD_EDGE,
	ALLOW_ADD_NODE,
	ADD_NODE,
	ADD_EDGE
} from '../../redux/action_types'

const graphListener = (state: InitialState) => {

}